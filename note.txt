First the easy way to push your project to github you should use VsCode.

Open the project in VsCode....
Then open the terminal
Type git init, then git add .
After that you will see some numbers on the git branch section showing the changes which are waiting to be commited...
Type a message then commit your repository.